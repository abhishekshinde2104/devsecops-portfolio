# Copy to .env (git-ignored) and fill in. Generate the key with:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
APP_SECRET_KEY=
APP_ENV=dev
APP_DOCS_ENABLED=true
APP_DATABASE_URL=sqlite:///./app.db
# Optional: restrict URL previews to specific hosts (JSON list)
# APP_OUTBOUND_ALLOWED_HOSTS=["pay.example.com"]
