# INSECURE BASELINE: exposed on every node's IP via NodePort.
apiVersion: v1
kind: Service
metadata:
  name: invoice-api
  namespace: default
spec:
  type: NodePort
  selector:
    app: invoice-api
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30080
